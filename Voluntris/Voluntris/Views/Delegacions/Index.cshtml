@model IEnumerable<Voluntris.Models.Delegacio>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <!--
        <th>
            @Html.DisplayNameFor(model => model.AdministradorDelegacio.PasswordHash)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ID)
        </th>

         -->
        <th>
            @Html.DisplayNameFor(model => model.NomDelegacio)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <!--
        <td>
            @Html.DisplayFor(modelItem => item.AdministradorDelegacio.PasswordHash)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ID)
        </td>
         -->
        <td>
            @Html.DisplayFor(modelItem => item.NomDelegacio)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AdministradorDelegacioID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AdministradorDelegacioID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AdministradorDelegacioID })
        </td>
    </tr>
}

</table>
