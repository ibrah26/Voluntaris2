@model IEnumerable<Voluntris.Models.Categoria>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <!--
        <th>
            @Html.DisplayNameFor(model => model.AdministradorCategoria.PasswordHash)
        </th>
            -->
        <th>
            @Html.DisplayNameFor(model => model.NomCategoria)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DescripcioCategoria)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ImatgeCategoria)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <!--
        <td>
            @Html.DisplayFor(modelItem => item.AdministradorCategoria.PasswordHash)
        </td>
            -->
        <td>
            @Html.DisplayFor(modelItem => item.NomCategoria)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DescripcioCategoria)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ImatgeCategoria)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
